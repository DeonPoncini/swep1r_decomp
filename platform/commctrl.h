#ifndef PLATFORM_COMMCTRL_H
#define PLATFORM_COMMCTRL_H

// this used for the functions from COMMCTL32.DLL
// This is the windows library for interacting through the UI

#include "types.h"

void __stdcall InitCommonControls(void);

#endif
